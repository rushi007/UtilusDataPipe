apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "utilusdatapipe.fullname" . }}-test-connection"
  labels:
    {{- include "utilusdatapipe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "utilusdatapipe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
